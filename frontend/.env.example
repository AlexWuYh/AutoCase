# Frontend environment template. Copy to .env.local for dev or pass via docker-compose.
# Default API proxy target for `npm run dev`.
VITE_API_PROXY_TARGET=http://localhost:8000

# API base path (same as backend prefix)
VITE_API_BASE=/api/v1
